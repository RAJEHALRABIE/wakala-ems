# Wakala-EMS: تقرير تسليم هندسي

## 1. ملخص تنفيذي

### 1.1. تعريف التطبيق وأهدافه

**Wakala EMS** هو نظام إدارة متخصص، مصمم لتسهيل ومتابعة قضايا نزع الملكية للمنفعة العامة. التطبيق يهدف إلى خدمة فرق العمل القانونية والإدارية التي تمثل العملاء (مُلّاك العقارات)، حيث يوفر منصة مركزية لتسجيل بيانات العملاء، تفاصيل عقاراتهم، المستندات المطلوبة، ومتابعة حالة كل قضية بدقة عبر مراحلها المختلفة، من المراجعة الأولية وحتى استلام التعويض والإغلاق النهائي للملف.

### 1.2. موجز نقاط القوة والتحديات

-   **نقاط القوة:**
    -   **بنية تقنية حديثة:** يعتمد النظام على حزمة تقنيات متكاملة (React, Node.js, TypeScript, tRPC) تضمن سلامة الأنواع (Type-Safety) من الواجهة الأمامية إلى قاعدة البيانات، مما يسرّع التطوير ويقلل الأخطاء.
    -   **تجربة تطوير متكاملة:** استخدام tRPC يلغي الحاجة إلى تعريفات API منفصلة، مما يجعل الاتصال بين العميل والخادم سلسًا وقويًا.
    -   **أساس قابل للتوسع:** الهيكل الحالي للنظام يسمح بإضافة وحدات وظيفية جديدة بسهولة نسبيًا.

-   **أهم المخاطر والتحديات:**
    -   **ثغرة أمنية حرجة:** آلية المصادقة الحالية بدائية وتعتمد على رمز وصول ثابت، مما يعرض جميع بيانات النظام لخطر الوصول والتلاعب غير المصرح به.
    -   **تعقيد الصيانة:** المكون المسؤول عن نموذج إدخال بيانات العميل (`ClientForm`) ضخم ومعقد (God Component)، مما يجعل صيانته وتطويره أمرًا صعبًا ومحفوفًا بالمخاطر.
    -   **غياب الاختبارات الآلية:** عدم وجود استراتيجية اختبار واضحة يرفع من احتمالية ظهور أخطاء تراجعية (regressions) عند إجراء أي تعديلات على الكود.

## 2. البنية المعمارية وطبقات النظام

يعتمد التطبيق على بنية متكاملة (Monolithic) موزعة على طبقات منطقية واضحة، مما يسهل فهم تدفق البيانات والمسؤوليات.

-   **الطبقة الأولى: الواجهة الأمامية (Client-Side Application)**
    -   **الوصف:** تطبيق من صفحة واحدة (SPA) مسؤول عن عرض واجهات المستخدم والتفاعل معها.
    -   **التقنيات:** `React` (v18) لبناء المكونات، `Vite` كأداة بناء وتشغيل خادم التطوير، `TypeScript` لضمان سلامة الأنواع، و `Wouter` لإدارة المسارات (Routing).

-   **الطبقة الثانية: الواجهة الخلفية (Server-Side API)**
    -   **الوصف:** خادم API مبني على Node.js، وهو المسؤول عن منطق الأعمال (Business Logic) والاتصال بقاعدة البيانات.
    -   **التقنيات:** `Express.js` كإطار عمل أساسي للخادم، `tRPC` لإنشاء واجهات برمجة تطبيقات آمنة الأنواع، و `Zod` للتحقق من صحة هياكل البيانات الواردة.

-   **الطبقة الثالثة: النواة المشتركة (Shared Kernel)**
    -   **الوصف:** مجلد (`/shared`) يحتوي على كود TypeScript مشترك بين الواجهتين الأمامية والخلفية. هذا يشمل تعريفات الأنواع (`types.ts`)، الثوابت (مثل حالات الملفات في `statuses.ts` وأنواع المستندات في `documents.ts`)، والوظائف المساعدة. هذا التصميم يضمن التناسق ويمنع تكرار الكود.

-   **الطبقة الرابعة: طبقة استمرارية البيانات (Data Persistence Layer)**
    -   **الوصف:** مسؤولة عن التفاعل مع قاعدة البيانات (إنشاء، قراءة، تحديث، حذف).
    -   **التقنيات:** `Drizzle ORM`، وهو ORM حديث وخفيف متوافق تمامًا مع TypeScript، لإدارة مخططات قاعدة البيانات وتنفيذ الاستعلامات. يتم تعريف الجداول في `drizzle/schema.ts`.

## 3. واجهات المستخدم والشاشات

فيما يلي حصر لجميع الشاشات الرئيسية في النظام ودور كل منها.

-   **شاشة تسجيل الدخول (Login Page)**
    -   **الغرض:** نقطة الدخول إلى النظام. تتطلب من المستخدم إدخال رمز وصول للتحقق من هويته.
    -   **المسار (Route):** `/login`
    -   **الوصول:** يتم توجيه المستخدمين غير المصادق عليهم إلى هذه الصفحة تلقائيًا.

-   **لوحة التحكم (Dashboard)**
    -   **الغرض:** الصفحة الرئيسية بعد تسجيل الدخول. تعرض ملخصًا مرئيًا لحالة جميع ملفات العملاء (على سبيل المثال، عدد الملفات في كل مرحلة).
    -   **المسار (Route):** `/`
    -   **الوصول:** الصفحة الأولى التي يراها المستخدم بعد تسجيل الدخول. يمكن الوصول إليها أيضًا عبر رابط في الشريط الجانبي.

-   **قائمة العملاء (Client List)**
    -   **الغرض:** عرض جميع ملفات العملاء في جدول منظم. تتيح للمستخدم البحث والفلترة للعثور على ملفات محددة بسرعة.
    -   **المسار (Route):** `/clients`
    -   **الوصول:** عبر رابط "العملاء" في الشريط الجانبي.

-   **نموذج العميل (Client Form)**
    -   **الغرض:** الشاشة المحورية لإنشاء ملف عميل جديد أو تعديل ملف موجود. تحتوي على جميع الحقول اللازمة لتوثيق قضية العميل.
    -   **المسار (Route):** `/client/new` (لإنشاء جديد) و `/client/:id` (لتعديل موجود).
    -   **الوصول:** عن طريق زر "إضافة عميل" في صفحة قائمة العملاء، أو بالنقر على سجل عميل موجود في الجدول.

## 4. الأقسام الداخلية والتبويبات

شاشة **نموذج العميل (`ClientForm`)** هي الشاشة الوحيدة متعددة الأقسام حاليًا. تم تقسيمها بصريًا لتسهيل إدخال البيانات، على النحو التالي:

-   **قسم المعلومات الأساسية:** يحتوي على بيانات العميل الشخصية وبيانات الوكالة.
-   **قسم حالة الملف:** حقل واحد لتحديد المرحلة الحالية للقضية.
-   **قسم معلومات العقار:** تفاصيل صك الملكية وموقع العقار الجغرافي.
-   **قسم المستندات:** قائمة تحقق لتتبع المستندات المستلمة من العميل.
-   **قسم الإحداثيات:** واجهة خريطة لتحديد موقع العقار بدقة.
-   **قسم مكونات العقار (التحسينات):** قسم ديناميكي لإضافة وتوصيف المباني أو أي إنشاءات أخرى على أرض العقار.

## 5. جداول الحقول والقوائم لكل شاشة

### 5.1. شاشة تسجيل الدخول

| اسم الحقل في الكود | نوع الحقل | التسمية الظاهرة | الوصف الوظيفي | القيم الممكنة |
| :--- | :--- | :--- | :--- | :--- |
| `accessCode` | نص (Password) | رمز الدخول | يستخدم للتحقق من هوية المستخدم والسماح له بالوصول للنظام. | نص سري. |

### 5.2. شاشة قائمة العملاء (أدوات الفلترة)

| اسم الحقل في الكود | نوع الحقل | التسمية الظاهرة | الوصف الوظيفي | القيم الممكنة |
| :--- | :--- | :--- | :--- | :--- |
| `searchTerm` | نص (Text) | بحث... | للبحث عن عميل بالاسم أو رقم الهوية أو الهاتف. | أي نص. |
| `statusFilter` | اختيار من قائمة | تصفية حسب الحالة | لعرض العملاء الذين تتوافق حالة ملفاتهم مع القيمة المختارة. | `الكل`، وقائمة الحالات الكاملة. |
| `dateRange` | محدد تاريخ مزدوج | تصفية حسب التاريخ | لتحديد نطاق زمني وعرض الملفات التي تم إنشاؤها خلاله. | أي نطاق تاريخ صحيح. |

### 5.3. شاشة نموذج العميل

#### القسم 1: المعلومات الأساسية

| اسم الحقل في الكود | نوع الحقل | التسمية الظاهرة | الوصف الوظيفي | القيم الممكنة |
| :--- | :--- | :--- | :--- | :--- |
| `clientName` | نص (Text) | اسم العميل | الاسم الكامل للمالك كما في الوثائق الرسمية. | نص. |
| `clientPhone` | نص (Text) | رقم الهاتف | رقم الاتصال الأساسي مع العميل. | نص (يفضل أرقام). |
| `clientNationalId`| نص (Text) | رقم الهوية | رقم الهوية الوطنية للمالك. | نص (10 أرقام). |
| `wakalaNumber` | نص (Text) | رقم الوكالة | رقم الوكالة الشرعية في حال كان المراجع وكيلًا. | نص. |
| `wakalaDate` | تاريخ (Date) | تاريخ الوكالة | تاريخ إصدار الوكالة الشرعية. | تاريخ صحيح. |

#### القسم 2: حالة الملف

| اسم الحقل في الكود | نوع الحقل | التسمية الظاهرة | الوصف الوظيفي | القيم الممكنة وشرحها |
| :--- | :--- | :--- | :--- | :--- |
| `status` | اختيار من قائمة | حالة الملف | يحدد المرحلة الإجرائية الحالية لملف العميل. | **`PENDING_REVIEW`**: ملف جديد بانتظار المراجعة الأولية.<br>**`DATA_ENTRY`**: مرحلة إكمال إدخال البيانات في النظام.<br>**`LEGAL_REVIEW`**: الملف قيد المراجعة من قبل الفريق القانوني.<br>**`APPRAISAL`**: مرحلة تقييم العقار وتثمين قيمته.<br>**`COMPENSATION_OFFER`**: تم تقديم عرض تعويض للعميل.<br>**`NEGOTIATION`**: مرحلة التفاوض على قيمة التعويض.<br>**`AWAITING_PAYMENT`**: تم التوصل لاتفاق، وفي انتظار صرف المبلغ.<br>**`PAYMENT_RECEIVED`**: استلم العميل مبلغ التعويض.<br>**`CLOSED`**: تم إغلاق الملف بنجاح.<br>**`ARCHIVED`**: تم أرشفة الملف للرجوع إليه مستقبلًا. |

#### القسم 3: معلومات العقار

| اسم الحقل في الكود | نوع الحقل | التسمية الظاهرة | الوصف الوظيفي | القيم الممكنة |
| :--- | :--- | :--- | :--- | :--- |
| `property.deedNumber`| نص (Text) | رقم الصك | رقم صك ملكية العقار. | نص. |
| `property.pieceNumber`| نص (Text) | رقم القطعة | رقم قطعة الأرض في المخطط. | نص. |
| `property.planNumber` | نص (Text) | رقم المخطط | رقم المخطط التنظيمي للعقار. | نص. |
| `property.city` | نص (Text) | المدينة | اسم المدينة. | نص. |
| `property.district` | نص (Text) | الحي | اسم الحي. | نص. |
| `property.notes` | نص طويل (Textarea) | ملاحظات | أي تفاصيل إضافية حول العقار. | نص. |
| `property.coords` | إحداثيات (Map) | إحداثيات العقار | لتحديد الموقع الجغرافي الدقيق للعقار. | [خط عرض, خط طول]. |

#### القسم 4: المستندات

| اسم الحقل في الكود | نوع الحقل | التسمية الظاهرة | الوصف الوظيفي | القيم الممكنة وشرحها |
| :--- | :--- | :--- | :--- | :--- |
| `property.docs` | اختيار متعدد | المستندات المستلمة | قائمة تحقق لتتبع الوثائق المستلمة من العميل. | **`صورة الهوية الوطنية`**: هوية المالك.<br>**`صك الملكية`**: وثيقة ملكية العقار.<br>**`الوكالة الشرعية`**: في حال وجود وكيل.<br>**`محضر التبليغ`**: إشعار العميل بقرار النزع.<br>**`قرار النزع`**: القرار الحكومي الرسمي.<br>**`محضر الحصر`**: وثيقة حصر مكونات العقار.<br>**`كروكي العقار`**: رسم تخطيطي لموقع العقار.<br>**`شهادة إحداثيات`**: وثيقة رسمية بالإحداثيات.<br>**`صور للعقار`**: صور فوتوغرافية للعقار. |

#### القسم 5: مكونات العقار

| اسم الحقل في الكود | نوع الحقل | التسمية الظاهرة | الوصف الوظيفي | القيم الممكنة |
| :--- | :--- | :--- | :--- | :--- |
| `improvement.type` | اختيار من قائمة | نوع المكون | لتصنيف نوع البناء أو التحسين على الأرض. | `مبنى`, `فيلا`, `سور`, `خزان`, `أخرى`. |
| `improvement.description`| نص (Text) | وصف المكون | تفاصيل إضافية عن المكون (مثل عدد الأدوار). | نص. |
| `improvement.area` | رقم (Number) | المساحة (م²) | مساحة المكون بالمتر المربع. | رقم. |

## 6. خارطة عمل التطبيق (Application Workflow Map)

يمر المستخدم ومعه ملف العميل بالرحلة النموذجية التالية داخل النظام:

1.  **الدخول للنظام:** يبدأ المستخدم بزيارة صفحة `/login` وإدخال رمز الوصول.
2.  **نظرة عامة:** بعد النجاح، يتم توجيهه إلى لوحة التحكم (`/`) للحصول على ملخص سريع لحالة جميع الملفات.
3.  **الانتقال لقائمة العملاء:** ينتقل المستخدم إلى شاشة قائمة العملاء (`/clients`) عبر الشريط الجانبي لاستعراض جميع الملفات.
4.  **إنشاء ملف جديد:** يضغط المستخدم على زر "إضافة عميل"، مما ينقله إلى نموذج العميل الفارغ (`/client/new`).
5.  **إدخال البيانات المبدئي:** يقوم المستخدم بملء جميع البيانات المتاحة في نموذج العميل، بدءًا من معلومات العميل الأساسية، تفاصيل العقار، ورفع المستندات المتوفرة. يتم تحديد الحالة المبدئية للملف (غالبًا `PENDING_REVIEW` أو `DATA_ENTRY`).
6.  **حفظ الملف:** عند الحفظ، يتم إنشاء سجل جديد في قاعدة البيانات، ويعود المستخدم إلى قائمة العملاء.
7.  **المتابعة والتحديث:** مع تقدم القضية، يعود المستخدم إلى ملف العميل (عبر النقر عليه في قائمة العملاء، مما ينقله إلى `/client/:id`) لتحديث حالته. على سبيل المثال، تغيير الحالة من `LEGAL_REVIEW` إلى `APPRAISAL`.
8.  **الإغلاق أو الأرشفة:** بعد انتهاء القضية واستلام العميل للتعويض، يقوم المستخدم بتحديث حالة الملف إلى `PAYMENT_RECEIVED` ثم `CLOSED` أو `ARCHIVED`، لإكمال دورة حياة الملف.

## 7. الربط مع طبقة البيانات

تترجم النماذج في الواجهة الأمامية مباشرة إلى جداول في قاعدة البيانات عبر Drizzle ORM:

-   **العميل (`ClientForm`):** حقول مثل `clientName` و `clientNationalId` ترتبط بجدول `clients`.
-   **العقار:** بيانات العقار مثل `deedNumber` و `planNumber` ترتبط بجدول `properties`، الذي يرتبط بدوره بجدول `clients` في علاقة واحد لواحد (One-to-One).
-   **المكونات/التحسينات:** كل عنصر في قائمة مكونات العقار يمثل صفًا في جدول `improvements`، وهذا الجدول يرتبط بجدول `properties` في علاقة واحد لكثير (One-to-Many).
-   **المستندات والحالات:** يتم تخزينها غالبًا كحقول نصية أو JSON ضمن جدول `properties`، مما يعكس مرونة في التعامل مع هذه البيانات.

## 8. تقييم جودة الكود والصيانة

-   **التنظيم:** هيكل المشروع منظم بشكل جيد ومنطقي، مما يسهل على المطورين الجدد فهمه. فصل الكود إلى `client`, `server`, و `shared` هو ممارسة ممتازة.
-   **المكونات الضخمة:** يعتبر ملف `client/src/pages/ClientForm.tsx` مثالًا واضحًا على نمط "God Component". حجمه الكبير وتعدد مسؤولياته (إدارة الحالة، العرض، التحقق) يجعل من الصعب اختباره أو تعديله بأمان. تفكيك هذا المكون إلى مكونات أصغر ومتخصصة أمر ضروري.
-   **الاختبارات الآلية:** يفتقر المشروع بشكل شبه كامل للاختبارات الآلية (Unit, Integration). هذا الغياب يعني أن أي تغيير، مهما كان بسيطًا، قد يؤدي إلى تعطل وظائف قائمة دون اكتشاف ذلك إلا يدويًا، مما يزيد من تكلفة الصيانة ويقلل من موثوقية النظام.

## 9. تقييم الأمان والمخاطر

-   **المصادقة والتفويض:** هذه هي نقطة الضعف الأكثر خطورة. الاعتماد على متغير بيئة واحد (`ACCESS_CODE`) كآلية حماية يعني أنه بمجرد تسرب هذا الرمز، يصبح النظام بأكمله مكشوفًا. لا يوجد تمييز بين المستخدمين أو صلاحياتهم، مما يسمح لأي شخص بالوصول إلى جميع البيانات وتنفيذ جميع العمليات.
-   **حماية البيانات:** لا توجد إشارة إلى تشفير البيانات الحساسة أثناء التخزين (at-rest) أو تدقيق للتغييرات (audit logs)، مما يجعل من الصعب تتبع التعديلات غير المصرح بها.

## 10. التوصيات العملية

فيما يلي قائمة بالإجراءات الموصى بها، مرتبة حسب الأولوية:

1.  **أولوية عالية: إصلاح ثغرة المصادقة**
    -   **الإجراء:** التخلص الفوري من آلية `ACCESS_CODE` وتطبيق نظام مصادقة وتفويض حقيقي قائم على المستخدمين (User-based).
    -   **اقتراح:** استخدام حلول مثل `Lucia Auth` أو `Clerk`، أو بناء نظام مخصص باستخدام JWTs مع تخزين كلمات المرور بشكل مشفر (hashed). يجب أن يدعم النظام أدوارًا وصلاحيات مختلفة للمستخدمين.

2.  **أولوية عالية: تفكيك مكون `ClientForm`**
    -   **الإجراء:** إعادة هيكلة `ClientForm.tsx` إلى مكونات أصغر (مثل `ClientInfoForm`, `PropertyForm`, `DocumentsChecklist`).
    -   **اقتراح:** تبني مكتبة لإدارة النماذج مثل `React Hook Form` أو `Formik` لتبسيط منطق التحقق من الصحة وإدارة الحالة.

3.  **أولوية متوسطة: بناء استراتيجية اختبار**
    -   **الإجراء:** البدء في كتابة اختبارات آلية للوظائف الحرجة.
    -   **اقتراح:** كتابة اختبارات وحدة (Unit Tests) باستخدام `Vitest` لمنطق الأعمال في `server/routers.ts`، واختبارات تكامل (Integration Tests) للتحقق من صحة تدفق البيانات من إنشاء عميل حتى حفظه.

4.  **أولوية منخفضة: تحسين تجربة المستخدم (UX)**
    -   **الإجراء:** إضافة مؤشرات تحميل (loading indicators) عند تنفيذ عمليات الشبكة، ورسائل تأكيد أو خطأ واضحة (toasts/notifications) لإعلام المستخدم بنتيجة أفعاله.