# خطة العمل - Engineering Order: Database Core & Agency Validity Logic

## 1. تحليل المتطلبات
- [x] فحص المخطط الحالي لجدول `clients` في `drizzle/schema.ts`
- [x] فحص `clientDocuments.router.ts` لفهم آلية التسجيل الحالية
- [ ] تحديد الحقول الجديدة المطلوبة في جدول `clients`

## 2. تطوير المخطط (Schema Evolution)
### 2.1 تحديث جدول `clients`
- إضافة الحقول التالية كحقول اختيارية (optional):
  - `agencyIssueDate` (تاريخ إصدار الوكالة)
  - `agencyDurationDays` (مدة الوكالة بالأيام)
  - `agencyEndDate` (تاريخ انتهاء الوكالة)

### 2.2 إنشاء جدول `client_activity_log`
- إنشاء جدول جديد لتسجيل أنشطة العملاء يشمل:
  - `id` (المعرف الرئيسي)
  - `clientId` (معرف العميل)
  - `actionType` (نوع الإجراء: STATUS_CHANGE, DOC_UPLOAD, DOC_DELETE, WHATSAPP_SENT, NOTE_ADD)
  - `description` (وصف النشاط)
  - `meta` (بيانات إضافية بتنسيق JSONB)
  - `performedByUserId` (معرف المستخدم الذي قام بالإجراء)
  - `createdAt` (تاريخ الإنشاء)

### 2.3 إنشاء جدول `client_notes`
- إنشاء جدول للملاحظات الداخلية على مستوى العميل:
  - `id` (المعرف الرئيسي)
  - `clientId` (معرف العميل)
  - `note` (نص الملاحظة)
  - `createdBy` (معرف منشئ الملاحظة)
  - `createdAt` (تاريخ الإنشاء)

## 3. التنفيذ المنطقي
### 3.1 إنشاء أداة مساعدة `calculateAgencyStatus`
- دالة لحساب الأيام المتبقية حتى `agencyEndDate`
- معالجة الحالات الخاصة (تاريخ انتهاء مفقود، تاريخ سابق، إلخ)

### 3.2 تنفيذ منطق الحساب التلقائي لـ `agencyEndDate`
- حساب `agencyEndDate` تلقائيًا إذا توفر `agencyIssueDate` و `agencyDurationDays`
- إضافة التحقق من صحة المدخلات

### 3.3 إنشاء أداة `formatWhatsAppNumber`
- دالة لتنسيق أرقام الهواتف إلى تنسيق موحد `9665XXXXXXXX`
- معالجة الرموز الدولية والأصفار الزائدة

## 4. التكامل
### 4.1 إعداد مسجل عالمي (global logging helper)
- إنشاء دالة مساعدة لتسجيل الأنشطة في `client_activity_log`
- تكامل الدالة مع `clientDocuments.router` لتسجيل إجراءات "Upload" و "Delete"

### 4.2 تحديث مسار المستندات
- تعديل `clientDocuments.router.ts` لاستدعاء المسجل عند رفع أو حذف المستندات

## 5. ملفات المخطط (Schema Files)
### 5.1 تحديث `drizzle/schema.ts`
- إضافة الحقول الجديدة لجدول `clients`
- تعريف جدولي `client_activity_log` و `client_notes`

### 5.2 إنشاء ملفات الهجرة (Migrations)
- إنشاء ملف هجرة Drizzle لتطبيق التغييرات على قاعدة البيانات

## 6. الاختبار
- [ ] اختبار الحقول الجديدة في واجهة المستخدم
- [ ] اختبار دالة `calculateAgencyStatus`
- [ ] اختبار تنسيق أرقام الواتساب
- [ ] اختبار نظام تسجيل الأنشطة

## 7. التوثيق
- [ ] تحديث توثيق API إذا لزم الأمر
- [ ] تحديث دليل المطورين

## الجدول الزمني المقترح
1. **اليوم 1**: تحديث المخطط وإنشاء الجداول الجديدة
2. **اليوم 2**: تنفيذ الأدوات المساعدة والمنطق
3. **اليوم 3**: التكامل مع نظام المستندات الحالي والاختبار

## الاعتبارات التقنية
- جميع الحقول يجب أن تكون اختيارية (optional) وفقًا لـ `.clinerules`
- الحفاظ على التوافق مع الإصدارات السابقة
- استخدام أنواع البيانات المناسبة (Date, Integer, JSONB)
- معالجة الأخطاء والسيناريوهات الحدية
